# Ник: Хакатон 32
# t32_hakaton_bot
# MAX Focus Campus (max_campus_bot) 🎓

Асинхронный чат-бот для мессенджера MAX, который помогает студентам кампуса держать учебу под контролем: собирать дедлайны, запускать фокус-сессии по технике Pomodoro, напоминать о важных заданиях и показывать статистику продуктивности.

---

## 📌 Описание проекта

**MAX Focus Campus (max_campus_bot)** — это учебный ассистент внутри мессенджера.  
Он помогает студенту:

- пройти короткий онбординг и сформировать профиль (вуз, группа, роль, предметы);
- автоматически извлекать дедлайны из обычных текстовых сообщений;
- запускать фокус-сессии (Pomodoro) с разной длительностью;
- получать напоминания о дедлайнах в ключевые моменты;
- смотреть свою статистику: сколько задач выполнено и сколько времени реально было в фокусе.

Бот построен на основе библиотеки **`aiomax`** и работает асинхронно.  
Данные хранятся в простых JSON-файлах (`data/users.json`, `data/tasks.json`, `data/focus_sessions.json`), что упрощает развёртывание и тестирование — отдельная БД не требуется.

Архитектура разделена на уровни:

- **`main`** — инициализация и запуск бота, регистрация роутеров и сервисов;
- **`routers/`** — обработчики команд и сценариев (`onboarding`, `focus`, `deadlines`, `schedule`);
- **`services/`** — фоновые и вспомогательные сервисы (`ReminderService`, статистика, защита состояний, NLP-парсер);
- **`database/`** — модели и хранилища (`UserStorage`, `TaskStorage`, `FocusStorage`), работающие поверх JSON-файлов.

---

## 👥 Целевая аудитория и решаемая проблема

**Целевая аудитория:**

- студенты бакалавриата, магистратуры и аспирантуры;
- особенно те, кто учится на кампусе и живёт в постоянном информационном шуме;
- пользователи мессенджера MAX, которые предпочитают решать всё «не выходя из чата».

**Проблема:**

- студенты постоянно сталкиваются с:
  - большим количеством дедлайнов по разным предметам;
  - отсутствием прозрачной картины «что и когда сдавать»;
  - прокрастинацией и отсутствием фокуса;
- при этом они большую часть времени проводят в мессенджерах, а не в отдельных таск-менеджерах.

**Решение:**

MAX Focus Campus связывает дедлайны, фокус-сессии и статистику прямо в мессенджере, где студент и так живёт.  
Никаких дополнительных приложений: просто бот, который:

- сам распознаёт дедлайны в тексте;
- напоминает о важных датах;
- помогает работать по Pomodoro;
- показывает, что студент действительно сделал, а не только «планировал».

---

## ✨ Основные возможности

### 1. 🎓 Онбординг и профиль студента

Реализовано в `onboarding_router`:

- Запуск по команде `/start`.
- Пошаговая настройка профиля (FSM):
  1. Университет (МГУ, МФТИ, ВШЭ, МГТУ или любой другой).
  2. Группа/курс (свободный текст).
  3. Роль (первокурсник, бакалавр, магистр, аспирант) — `UserRole`.
  4. Ссылка на календарь (ICS / публичный календарь) или «пропустить».
  5. Список предметов — ввод через запятую, сохраняется как теги.
- После завершения онбординга флаг `onboarding_completed = True`, и пользователь получает приветственное сообщение и меню с быстрыми кнопками:
  - «📅 Добавить задание» / «📅 Добавить дедлайн»;
  - «🎯 Начать фокус» / «🎯 Начать фокус-сессию»;
  - «📊 Мой прогресс».

Повторный `/start` для уже настроенного пользователя открывает главное меню, не запуская онбординг заново.

---

### 2. 📅 Автоматический сбор дедлайнов из текста

Реализовано в `deadlines_router` + `services.nlp_parser`:

- Бот анализирует **обычные текстовые сообщения** (если это не команда и текст не слишком короткий).
- NLP-парсер (`extract_deadline_info`) пытается найти:
  - дату в формате `DD.MM.YYYY` или `DD/MM/YYYY`;
  - русские форматы вида `12 марта`, `5 ноября`;
  - фразы вида `через 3 дня`;
  - варианты `до 12.11`.
- Из сообщения извлекается:
  - примерное название задания (первые несколько слов);
  - дедлайн (объект `datetime`);
  - предполагаемый предмет (`guess_subject` по ключевым словам: математика, программирование, физика, английский и др.).
- Пользователю показывается карточка найденного дедлайна и задаётся вопрос «Добавить в систему?» с кнопками:
  - ✅ Добавить дедлайн;
  - ✏️ Редактировать;
  - ❌ Отмена.
- При подтверждении создаётся `Task` и сохраняется через `TaskStorage`:
  - задачи привязаны к конкретному пользователю;
  - хранятся в `data/tasks.json`;
  - учитывают статус (`TaskStatus`), приоритет, оценочное количество Pomodoro и т.д.
- Команда `/deadlines` и кнопка **«📅 Мои дедлайны»** показывают ближайшие дедлайны на 30 дней:
  - название задания;
  - предмет;
  - дата дедлайна;
  - сколько дней осталось;
  - цветовой индикатор (🟢/🟡/🔴) по близости.

---

### 3. 🎯 Фокус-сессии по технике Pomodoro

Реализовано в `focus_router` + `FocusSession`:

- Команда `/focus` и кнопки:
  - «🎯 Начать фокус»;
  - «🎯 Начать фокус-сессию»;
  - «🔄 Новая сессия».
- Пользователь выбирает длительность:
  - 🍅 25 минут — классический Pomodoro;
  - 🔥 50 минут — глубокая работа;
  - ⚡ 15 минут — быстрая задача.
- Создаётся `FocusSession`, которая сохраняется через `FocusStorage` в `data/focus_sessions.json`.
- FSM переводит пользователя в состояние `FocusState.WORKING`, в хранилище состояний сохраняется:
  - время начала;
  - длительность;
  - `session_id`;
  - счётчик pomodoro.
- Пользователь получает сообщение с:
  - длительностью;
  - временем окончания;
  - рекомендациями по фокусу (убрать отвлекающие факторы и т.п.).
- Асинхронный таймер (`focus_timer`) по истечении времени:
  - помечает сессию как завершённую (`completed = True`);
  - очищает состояние FSM для пользователя;
  - отправляет сообщение о завершении и предлагает:
    - «🔄 Новая сессия»;
    - «📊 Статистика».

---

### 4. ⏰ Умные напоминания о приближающихся дедлайнах

Реализовано в `services.reminder.ReminderService`:

- При запуске бота сервис стартует в фоне (`reminder_service.start()`).
- Каждую минуту:
  - обходятся все пользователи из `UserStorage`;
  - для каждого выбираются ближайшие дедлайны (`get_upcoming_deadlines(user_id, days=1)`);
  - для каждой задачи считается оставшееся время.
- Отправляются напоминания в три ключевые точки:
  - за **24 часа** до дедлайна;
  - за **3 часа**;
  - за **30 минут**.
- Напоминание содержит:
  - название задания;
  - точную дату и время дедлайна;
  - сколько осталось;
  - мотивационное сообщение.

Все ошибки в фоне логируются через `logging` с отдельным логгером `max_focus_campus.reminder`.

---

### 5. 📊 Статистика продуктивности

Реализовано в `services.statistics`:

- Команда `/stats` и кнопки:
  - «📊 Статистика»;
  - «📊 Мой прогресс».
- В `send_stats_message` собирается статистика по пользователю:
  - количество выполненных задач (`TaskStatus.COMPLETED`);
  - количество завершённых фокус-сессий;
  - суммарное время в фокусе (в минутах);
  - количество активных дедлайнов (`TaskStatus.PENDING`).
- Пользователь получает аккуратный отчёт с ключевыми метриками.

---

### 6. 📚 Просмотр учебного профиля и расписания

Реализовано в `schedule_router`:

- Команда `/schedule` показывает текущие настройки профиля:
  - вуз;
  - группа;
  - список предметов;
  - состояние календаря (подключен/не подключен).
- В том же сообщении предлагаются действия:
  - `/deadlines` — показать дедлайны;
  - `/focus` — начать фокус-сессию;
  - `/stats` — статистика продуктивности.
- Кнопки:
  - «📅 Мои дедлайны»;
  - «🎯 Начать фокус»;
  - «📊 Статистика».

---

### 7. 🔒 Управление сценариями и состояниями

Реализовано в `services.state_guard`:

- Используется FSM (`FSMCursor`) из `aiomax.fsm` для управления сценариями:
  - онбординг (`OnboardingState`);
  - выбор длительности фокуса (`FocusState.SELECT_DURATION`);
  - состояния работы/перерыва.
- Функция `ensure_command_allowed` не даёт пользователю «ломать» сценарий:
  - если есть активное состояние, а команда не входит в список разрешённых состояний, бот просит завершить текущий шаг.
- Это защищает от случайных команд посреди онбординга или диалога, когда пользователь нажимает всё подряд.

---

## 🧑‍💻 Технический стек

- **Язык:** Python 3.10+  
- **Фреймворк/библиотека бота:** `aiomax`  
- **Управление состояниями:** `aiomax.fsm` (FSMCursor)  
- **Хранение данных:**
  - `UserStorage` — пользователи (`data/users.json`);
  - `TaskStorage` — задачи и дедлайны (`data/tasks.json`);
  - `FocusStorage` — фокус-сессии (`data/focus_sessions.json`).
- **Конфигурация:** `python-dotenv` (`.env` + `Config`).
- **Логирование:** стандартный модуль `logging` (лог в `bot.log` + вывод в консоль).
- **Фоновые задачи:** `asyncio` (таймеры, ReminderService).

---

## 📁 Структура проекта (основные модули)

```text
.
├── main.py                 # Точка входа, инициализация бота и сервисов
├── config.py               # Загрузка BOT_TOKEN из .env
├── database/
│   ├── __init__.py         # Инициализация хранилищ user/task/focus
│   ├── models.py           # User, Task, FocusSession, перечисления ролей и статусов
│   └── storage.py          # UserStorage, TaskStorage, FocusStorage (JSON-хранилища)
├── routers/
│   ├── onboarding.py       # Онбординг и первичная настройка профиля
│   ├── focus.py            # Фокус-сессии (Pomodoro)
│   ├── deadlines.py        # Обработка дедлайнов из текста и вывод списка
│   └── schedule.py         # Просмотр профиля и расписания
├── services/
│   ├── reminder.py         # Сервис напоминаний о дедлайнах
│   ├── nlp_parser.py       # Извлечение дедлайнов и предметов из текста
│   ├── state_guard.py      # Проверка допустимости команд при активном сценарии
│   └── statistics.py       # Формирование статистики продуктивности
└── data/
    ├── users.json          # Данные пользователей (создаётся автоматически)
    ├── tasks.json          # Задачи и дедлайны
    └── focus_sessions.json # История фокус-сессий
