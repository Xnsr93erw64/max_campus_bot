# Ник: Хакатон 32
# t32_hakaton_bot
# MAX Focus Campus (max_campus_bot / t32_hakaton_bot) 🎓

Асинхронный чат-бот для мессенджера MAX, который помогает студентам кампуса держать учебу под контролем: собирать дедлайны, запускать фокус-сессии по технике Pomodoro, напоминать о важных заданиях и показывать статистику продуктивности.

Бот развернут как сервис `t32_hakaton_bot` на платформе MAX.

---

## 📌 Описание проекта

**MAX Focus Campus (max_campus_bot)** — это учебный ассистент внутри мессенджера MAX.  
Он помогает студенту:

- пройти короткий онбординг и сформировать профиль (вуз, группа, роль, предметы);
- автоматически извлекать дедлайны из обычных текстовых сообщений;
- запускать фокус-сессии (Pomodoro) с разной длительностью;
- получать напоминания о дедлайнах в ключевые моменты;
- смотреть свою статистику: сколько задач выполнено и сколько времени реально было в фокусе.

Бот построен на основе библиотеки **`aiomax`** и работает асинхронно.  
Данные хранятся в простых JSON-файлах (`data/users.json`, `data/tasks.json`, `data/focus_sessions.json`), что упрощает развёртывание и тестирование — отдельная БД не требуется.

Архитектура разделена на уровни:

- **`main.py`** — инициализация и запуск бота, регистрация роутеров и сервисов;
- **`routers/`** — обработчики команд и сценариев (`onboarding`, `focus`, `deadlines`, `schedule`);
- **`services/`** — фоновые и вспомогательные сервисы (`ReminderService`, статистика, защита состояний, NLP-парсер);
- **`database/`** — модели и хранилища (`UserStorage`, `TaskStorage`, `FocusStorage`), работающие поверх JSON-файлов.

---

## 👥 Целевая аудитория и решаемая проблема

**Целевая аудитория:**

- студенты бакалавриата, магистратуры и аспирантуры;
- активные пользователи цифровых сервисов и мессенджера MAX;
- люди с высокой учебной нагрузкой и плотным графиком.

**Проблема:**

- задания и дедлайны приходят в разных чатах и сервисах;
- дедлайны теряются в переписках и скриншотах;
- нет простой общей картины нагрузки на ближайшие недели;
- при работе не хватает фокуса — мессенджер сам по себе отвлекающий.

**Решение:**

`t32_hakaton_bot` связывает дедлайны, фокус-сессии и статистику прямо в мессенджере, где студент и так живёт.  
Никаких дополнительных приложений: бот сам распознаёт дедлайны в тексте, напоминает о важных сроках, помогает работать в фокусе и показывает реальный прогресс.

---

## ✨ Основные возможности

### 1. 🎓 Онбординг и профиль студента

Реализовано в `onboarding_router`:

- Запуск по команде `/start`.
- Пошаговая настройка профиля:
  1. университет (МГУ, МФТИ, ВШЭ, МГТУ или другой);
  2. группа/курс;
  3. учебная роль (первокурсник, бакалавр, магистр, аспирант — `UserRole`);
  4. ссылка на календарь (или «пропустить»);
  5. список предметов (теги через запятую).
- После онбординга выставляется `onboarding_completed = True`, пользователь получает главное меню с кнопками:
  - «📅 Добавить задание» / «📅 Добавить дедлайн»;
  - «🎯 Начать фокус»;
  - «📊 Мой прогресс».

---

### 2. 📅 Автоматический сбор дедлайнов из текста

Реализовано в `deadlines_router` + `services.nlp_parser`:

- Бот анализирует обычные текстовые сообщения (если это не команда и не слишком короткий текст).
- NLP-парсер `extract_deadline_info` поддерживает:
  - даты `DD.MM.YYYY` и `DD/MM/YYYY`;
  - русские даты вида `12 марта`, `5 ноября`;
  - выражения `через 3 дня`;
  - конструкции `до 12.11`.
- Из сообщения извлекается:
  - примерное название задания (первые несколько слов);
  - дедлайн (`datetime`);
  - предположительный предмет (`guess_subject` по ключевым словам: математика, программирование, физика, английский и др.).
- Пользователь получает карточку найденного дедлайна и выбор:
  - ✅ добавить;
  - ✏️ отредактировать;
  - ❌ отменить.
- При подтверждении создаётся объект `Task` и сохраняется через `TaskStorage` (файл `data/tasks.json`), дедлайны привязаны к конкретному пользователю.

Команда `/deadlines` и кнопка **«📅 Мои дедлайны»** показывают список ближайших дедлайнов на 30 дней с цветными индикаторами по срочности.

---

### 3. 🎯 Фокус-сессии по технике Pomodoro

Реализовано в `focus_router` + `FocusSession`:

- Команда `/focus` и кнопки:
  - «🎯 Начать фокус»;
  - «🎯 Начать фокус-сессию»;
  - «🔄 Новая сессия».
- Длительности на выбор:
  - 🍅 25 минут — стандартный Pomodoro;
  - 🔥 50 минут — глубокая работа;
  - ⚡ 15 минут — быстрая задача.
- Создаётся `FocusSession`, сохраняется через `FocusStorage` в `data/focus_sessions.json`.
- FSM переводит пользователя в состояние `FocusState.WORKING`, в хранилище состояния сохраняются:
  - время начала;
  - длительность;
  - `session_id`.
- Пользователь получает сообщение с временем окончания и рекомендациями по фокусу.
- Асинхронный таймер `focus_timer` по завершении сессии:
  - помечает её как завершённую (`completed = True`);
  - очищает состояние FSM;
  - отправляет сообщение «Фокус-сессия завершена» с кнопками «🔄 Новая сессия» и «📊 Статистика».

---

### 4. ⏰ Умные напоминания о приближающихся дедлайнах

Реализовано в `services.reminder.ReminderService`:

- Сервис запускается вместе с ботом (`reminder_service.start()`).
- Каждую минуту:
  - обходятся все пользователи из `UserStorage`;
  - выбираются ближайшие задачи через `get_upcoming_deadlines(user_id, days=1)`;
  - для каждой задачи вычисляется оставшееся время.
- Напоминания отправляются за:
  - 24 часа до дедлайна;
  - 3 часа;
  - 30 минут.
- Сообщение содержит:
  - название задания;
  - дату и время дедлайна;
  - сколько времени осталось;
  - мотивационную подпись.

Ошибки в фоне логируются через `logging` с пространством имён `max_focus_campus.reminder`.

---

### 5. 📊 Статистика продуктивности

Реализовано в `services.statistics`:

- Команда `/stats` и кнопки:
  - «📊 Статистика»;
  - «📊 Мой прогресс».
- Рассчитываются:
  - количество выполненных задач (`TaskStatus.COMPLETED`);
  - количество завершённых фокус-сессий;
  - общее время в фокусе (в минутах);
  - количество активных дедлайнов (`TaskStatus.PENDING`).
- Пользователь получает сводный отчёт по своей продуктивности.

---

### 6. 📚 Просмотр учебного профиля и расписания

Реализовано в `schedule_router`:

- Команда `/schedule` показывает:
  - вуз;
  - группу;
  - список предметов;
  - подключён ли календарь.
- В сообщении — подсказки:
  - `/deadlines` — список дедлайнов;
  - `/focus` — старт фокус-сессии;
  - `/stats` — статистика.

Кнопки:

- «📅 Мои дедлайны»;
- «🎯 Начать фокус»;
- «📊 Статистика» / «📊 Мой прогресс».

---

### 7. 🔒 Управление сценариями и состояниями

Реализовано в `services.state_guard`:

- Используется FSM (`FSMCursor` из `aiomax.fsm`) для сценариев:
  - онбординг (`OnboardingState`);
  - выбор длительности фокуса (`FocusState.SELECT_DURATION`);
  - состояния работы/перерыва.
- `ensure_command_allowed` не позволяет пользователю ломать сценарий:
  - если есть активное состояние и команда не разрешена, бот просит сначала завершить текущий шаг.
- Это делает поведение предсказуемым и устойчивым даже при активном вводе.

---

## 🧑‍💻 Технический стек

- **Язык:** Python 3.11  
- **Фреймворк/библиотека бота:** `aiomax`  
- **Асинхронность:** `asyncio`  
- **Управление состояниями:** `aiomax.fsm`  
- **Хранение данных:**
  - `UserStorage` — пользователи (`data/users.json`);
  - `TaskStorage` — задачи и дедлайны (`data/tasks.json`);
  - `FocusStorage` — фокус-сессии (`data/focus_sessions.json`).
- **Конфигурация:** `python-dotenv` (`.env` + `Config`).
- **Логирование:** стандартный `logging` (лог в `bot.log` + stdout).
- **Контейнеризация:** Docker (образ на основе `python:3.11-slim`).

---

## 📁 Структура проекта (основные модули)

```text
.
├── main.py                 # Точка входа: запуск бота и сервисов
├── config.py               # Загрузка BOT_TOKEN из .env
├── requirements.txt        # Список зависимостей
├── Dockerfile              # Описание Docker-образа
├── database/
│   ├── __init__.py         # Инициализация хранилищ user/task/focus
│   ├── models.py           # User, Task, FocusSession, перечисления ролей и статусов
│   └── storage.py          # UserStorage, TaskStorage, FocusStorage (JSON-хранилища)
├── routers/
│   ├── onboarding.py       # Онбординг и первичная настройка профиля
│   ├── focus.py            # Фокус-сессии (Pomodoro)
│   ├── deadlines.py        # Обработка дедлайнов из текста и список дедлайнов
│   └── schedule.py         # Просмотр профиля и расписания
├── services/
│   ├── reminder.py         # Сервис напоминаний о дедлайнах
│   ├── nlp_parser.py       # Извлечение дедлайнов и предметов из текста
│   ├── state_guard.py      # Проверка допустимости команд при активном сценарии
│   └── statistics.py       # Формирование статистики продуктивности
└── data/
    ├── users.json          # Данные пользователей (создаётся автоматически)
    ├── tasks.json          # Задачи и дедлайны
    └── focus_sessions.json # История фокус-сессий
